# 📊 System Architecture Diagram

## Complete Integration Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                         GOOGLE MEET                                │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │            ClassBoost Extension Overlay                   │    │
│  │                                                           │    │
│  │  ┌─────────────────────┐  ┌──────────────────────────┐  │    │
│  │  │ Engagement Meter    │  │ Pair & Post Chat Links  │  │    │
│  │  │    (Real-time)      │  │   (AI Quiz Generator)   │  │    │
│  │  └─────────────────────┘  └──────────────────────────┘  │    │
│  │                                                           │    │
│  │  ┌─────────────────────┐  ┌──────────────────────────┐  │    │
│  │  │   Generate GIF      │  │  Generate Flashcard      │  │    │
│  │  └─────────────────────┘  └──────────────────────────┘  │    │
│  └──────────────────────────────────────────────────────────┘    │
│                                                                    │
│  Chat: "📚 Pair Assignments - Machine Learning Quiz:"             │
│        "Alice & Bob: http://127.0.0.1:8000/chat/1234?quiz=5"      │
└────────────────┬───────────────────────────────────────────────────┘
                 │
                 │ User clicks "Pair & Post Chat Links"
                 │ Enters topic: "Machine Learning"
                 │
                 ▼
┌────────────────────────────────────────────────────────────────────┐
│                     CHROME EXTENSION                               │
│                                                                    │
│  ┌───────────────────────┐         ┌──────────────────────────┐  │
│  │   content.js          │◄────────│    background.js         │  │
│  │                       │         │  (Service Worker)        │  │
│  │  - Overlay UI         │  msg    │                          │  │
│  │  - Button handlers    │─────────►  - Message router        │  │
│  │  - Topic modal        │         │  - API gateway           │  │
│  │  - Pairing logic      │         │  - Broadcasting          │  │
│  │  - Chat posting       │         │                          │  │
│  └───────────────────────┘         └────────┬─────────────────┘  │
│                                              │                     │
└──────────────────────────────────────────────┼─────────────────────┘
                                               │
                  ┌────────────────────────────┼─────────────────────┐
                  │                            │                     │
                  ▼                            ▼                     ▼
     ┌────────────────────────┐  ┌─────────────────────┐  ┌──────────────────┐
     │   NODE.JS SERVER       │  │  FASTAPI QUIZ GEN   │  │  DJANGO BACKEND  │
     │   (Port 3000)          │  │   (Port 9000)       │  │   (Port 8000)    │
     │                        │  │                     │  │                  │
     │  Express + Socket.io   │  │  Python + Gemini    │  │  Python + WS     │
     │                        │  │                     │  │                  │
     │  Endpoints:            │  │  Endpoints:         │  │  Endpoints:      │
     │  ├─ /api/generate-quiz │──►  ├─ /generate-quiz  │  │  ├─ /api/quiz/   │
     │  ├─ /api/generateGif   │  │  └─ /health         │  │  │   create/     │
     │  ├─ /api/generateFlash │  │                     │  │  ├─ /api/quiz/   │
     │  ├─ /api/snapshot      │  │  LLM Integration:   │  │  │   {id}/       │
     │  └─ /api/classScore    │  │  └─ Google Gemini   │  │  ├─ /api/comp/  │
     │                        │  │     (5 MCQs)        │  │  │   create/     │
     │  Services:             │  │                     │  │  ├─ /chat/{id}/  │
     │  ├─ GIF Search (API)   │  │  Quiz Format:       │  │  └─ WebSocket   │
     │  ├─ Flashcard Gen      │  │  {                  │  │                  │
     │  │  (Gemini)           │  │    "mcq": [...]     │  │  Database:       │
     │  └─ Engagement Track   │  │  }                  │  │  ├─ Quiz Model   │
     └────────────────────────┘  └──────────┬──────────┘  │  ├─ CompSession  │
                                            │             │  └─ Scores       │
                                            │             └──────────────────┘
                                            ▼
                                  ┌──────────────────────┐
                                  │   GOOGLE GEMINI AI   │
                                  │                      │
                                  │  LLM Model:          │
                                  │  gemini-2.5-flash    │
                                  │                      │
                                  │  Input: Topic        │
                                  │  Output: 5 MCQs      │
                                  │                      │
                                  │  Format:             │
                                  │  - Question text     │
                                  │  - 4 Options         │
                                  │  - Correct answer    │
                                  └──────────────────────┘
```

---

## Data Flow: Quiz Generation & Student Pairing

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         FLOW DIAGRAM                                    │
└─────────────────────────────────────────────────────────────────────────┘

  START
    │
    ▼
┌──────────────────────────────────┐
│ 1. Teacher clicks "Pair & Post"  │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 2. Topic Modal Opens             │
│    Input: "Machine Learning"     │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 3. content.js → background.js    │
│    Message: generateQuiz         │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 4. background.js → Node.js       │
│    POST /api/generate-quiz       │
│    Body: { topic: "ML" }         │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 5. Node.js → FastAPI             │
│    POST /generate-quiz           │
│    Forward request               │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 6. FastAPI → Gemini LLM          │
│    Generate 5 MCQs about ML      │
│    Processing... (~5-10 sec)     │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 7. Gemini Returns Quiz           │
│    {                             │
│      mcq: [                      │
│        {                         │
│          question: "...",        │
│          options: [...],         │
│          answer: "..."           │
│        }, ... (5 total)          │
│      ]                           │
│    }                             │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 8. FastAPI → Node.js → Extension │
│    Quiz JSON returned            │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 9. content.js receives quiz      │
│    Stores in window.__generated  │
│    Quiz                          │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 10. Extract Meet participants    │
│     Found: [Alice, Bob, Charlie, │
│             Dana]                │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 11. Pair participants randomly   │
│     Pair 1: Alice & Bob          │
│     Pair 2: Charlie & Dana       │
│     Room IDs: 1234, 5678         │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 12. For each pair:               │
│     Convert quiz to Django       │
│     format                       │
│     {                            │
│       title: "ML Quiz",          │
│       questions: [...],          │
│       duration: 120,             │
│       difficulty: "medium"       │
│     }                            │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 13. POST → Django                │
│     /api/quiz/create/            │
│     Create quiz in DB            │
│     Returns quiz_id: 5           │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 14. Generate chat links          │
│     Link 1:                      │
│     127.0.0.1:8000/chat/1234     │
│     ?quiz=5                      │
│                                  │
│     Link 2:                      │
│     127.0.0.1:8000/chat/5678     │
│     ?quiz=6                      │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 15. Build chat message           │
│     "📚 Pair Assignments -       │
│      Machine Learning Quiz:      │
│                                  │
│      Alice & Bob:                │
│      127.0.0.1:8000/chat/1234    │
│      ?quiz=5                     │
│                                  │
│      Charlie & Dana:             │
│      127.0.0.1:8000/chat/5678    │
│      ?quiz=6"                    │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 16. Post to Google Meet chat     │
│     Inject into chat input       │
│     Click send button            │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 17. All students see message     │
│     in Meet chat                 │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 18. Students click their link    │
│     Opens Django chat page       │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 19. Django loads quiz            │
│     GET /api/quiz/5/             │
│     Displays 5 questions         │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 20. Students answer questions    │
│     Select MCQ options           │
│     Submit answers               │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 21. Django calculates score      │
│     Compare with correct answers │
│     Store in CompetitiveSession  │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 22. Determine winner             │
│     Higher score wins            │
│     Tie: faster time wins        │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ 23. Display results              │
│     Update leaderboard           │
│     Show winner                  │
└─────────────────────────────────┘
    │
    ▼
   END
```

---

## Component Communication Matrix

| Component | Communicates With | Protocol | Data Format |
|-----------|------------------|----------|-------------|
| content.js | background.js | Chrome Messages | JSON |
| background.js | Node.js (3000) | HTTP POST/GET | JSON |
| Node.js | FastAPI (9000) | HTTP POST | JSON |
| FastAPI | Gemini AI | HTTPS | JSON |
| content.js | Django (8000) | HTTP POST | JSON |
| Django | Database | ORM | SQL |
| Extension | Meet UI | DOM Injection | HTML/Text |

---

## Port Allocation

```
┌─────────────┬──────────────────────┬────────────────────────┐
│    Port     │      Service         │       Purpose          │
├─────────────┼──────────────────────┼────────────────────────┤
│    3000     │   Node.js Express    │  API Gateway           │
│    8000     │   Django Backend     │  Chat & Quiz Storage   │
│    9000     │   FastAPI Quiz Gen   │  AI Quiz Generation    │
└─────────────┴──────────────────────┴────────────────────────┘
```

---

## Service Dependencies

```
Chrome Extension
  │
  ├─► Requires: Node.js Server (port 3000)
  │   └─► Requires: FastAPI (port 9000)
  │       └─► Requires: Gemini API Key
  │
  └─► Requires: Django Backend (port 8000)
      └─► Requires: Database (SQLite/PostgreSQL)
```

---

## Error Handling Flow

```
Any Service Fails
    │
    ▼
Extension catches error
    │
    ├─► FastAPI offline → "Quiz generation unavailable"
    ├─► Django offline → "Chat platform unavailable"
    ├─► Node.js offline → "Connection failed"
    └─► Gemini API error → "AI service error"
    │
    ▼
Show user-friendly error
    │
    ▼
Log to console for debugging
    │
    ▼
Continue with other features
```

---

## State Management

```
┌────────────────────────────────────────────────────────┐
│               Global State Storage                     │
├────────────────────────────────────────────────────────┤
│                                                        │
│  Extension (Chrome Storage):                          │
│  ├─ User preferences                                  │
│  └─ Cached engagement data                            │
│                                                        │
│  Extension (Window):                                  │
│  ├─ window.__generatedQuiz (current quiz)             │
│  ├─ window.__quizTopic (current topic)                │
│  └─ window.__classboost_quizzes (in-flight quizzes)   │
│                                                        │
│  Node.js (Memory):                                    │
│  ├─ lastSnapshots (engagement data)                   │
│  └─ classSmoothed (smoothed score)                    │
│                                                        │
│  Django (Database):                                   │
│  ├─ Quiz (persistent storage)                         │
│  ├─ CompetitiveSession (match data)                   │
│  └─ User scores & history                             │
└────────────────────────────────────────────────────────┘
```

---

This diagram shows the complete architecture of your integrated system!
